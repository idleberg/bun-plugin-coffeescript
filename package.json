{
	"name": "bun-plugin-coffeescript",
	"version": "0.0.0",
	"description": "Bun plugin to support CoffeeScript files",
	"license": "MIT",
	"author": "Jan T. Sott",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/idleberg/node-package-template.git"
	},
	"keywords": [],
	"type": "module",
	"exports": {
		".": {
			"bun": "./src/index.ts",
			"types": "./src/module.d.ts",
			"import": "./src/index.ts"
		}
	},
	"engines": {
		"bun": ">=1.0.0"
	},
	"files": [
		"src/",
		"LICENSE",
		"README.md"
	],
	"scripts": {
		"lint": "concurrently --prefix-colors blue,magenta npm:lint:*",
		"lint:biome": "biome check",
		"lint:e18e": "e18e-cli analyze",
		"prepack": "npm run lint && npm run build",
		"prepare": "lefthook install",
		"publish:jsr": "deno publish",
		"publish:npm": "np --any-branch",
		"test": "bun test"
	},
	"dependencies": {
		"coffeescript": "^2.7.0"
	},
	"devDependencies": {
		"@commitlint/cli": "^20.1.0",
		"@commitlint/config-conventional": "^20.0.0",
		"@e18e/cli": "^0.3.1",
		"@idleberg/configs": "^0.3.3",
		"@types/coffeescript": "^2.5.7",
		"bun-types": "^1.3.2",
		"concurrently": "^9.2.1"
	}
}
